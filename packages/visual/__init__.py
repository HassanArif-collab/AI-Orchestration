"""
Visual package — programmatic video rendering.

Subpackages:
  remotion/  — Remotion (React-based) video animation config generation
  radiant/   — Canvas shader background selection and embedding

These produce rendering configs consumed by the JS Remotion renderer.
Python code here generates the config; the actual rendering is JS-side.
"""
