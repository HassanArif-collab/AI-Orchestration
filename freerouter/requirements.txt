# FreeRouter minimal dependencies (v3)
# These are already installed by the root project (pyproject.toml)
# Listed here for standalone/documentation purposes:

litellm>=1.52.0      # LLM routing (from crewai-deps)
fastapi>=0.111.0     # Web framework (from main deps)
uvicorn>=0.29.0      # ASGI server (from main deps)
python-dotenv>=1.1.0 # Environment variables (from main deps)
